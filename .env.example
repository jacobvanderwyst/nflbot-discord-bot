# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# NFL Data API Configuration (SportsData.io)
NFL_API_KEY=your_sportsdata_io_api_key_here
NFL_API_BASE_URL=https://api.sportsdata.io/v3/nfl

# Bot Settings
BOT_PREFIX=!
COMMAND_COOLDOWN=3
MAX_CONCURRENT_REQUESTS=10

# Role-Based Access Control
# BOT_ALLOWED_ROLE=Bot Users          # Role required to use any bot commands
# BOT_VISIBILITY_ROLE=VIP Members     # Controls slash command visibility (see below)

# 👁️ SLASH COMMAND VISIBILITY CONTROL
# BOT_VISIBILITY_ROLE determines who can see slash command responses:
#
# OPTION 1: Not set (default) = All commands are PUBLIC
# BOT_VISIBILITY_ROLE=
# Result: Both !stats and /stats responses visible to everyone
# Use case: Community servers where sharing NFL stats is encouraged
#
# OPTION 2: Set to any role name = Slash commands are EPHEMERAL (private)
# BOT_VISIBILITY_ROLE="Members"
# Result: !stats = public (everyone sees), /stats = private (only user sees)
# Use case: Clean channels - slash commands for personal use, ! commands for sharing
#
# 💡 Strategic Usage:
# - Use /stats for personal research (private to you)
# - Use !stats when you want to share with the channel (public)
BOT_VISIBILITY_ROLE=

# Database Configuration (if needed)
# DATABASE_URL=your_database_url_here

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=bot.log

# Update Intervals (in minutes)
STATS_UPDATE_INTERVAL=30
SCHEDULE_UPDATE_INTERVAL=1440
